import { createStyles } from "@mui/styles";


export const styles = createStyles({
    formInputContainer: {
        width: '100%',
    },
    formButton: {
        minHeight: 50,
        fontSize: 25,
        backgroundColor: '#23e9b4',
    },
});